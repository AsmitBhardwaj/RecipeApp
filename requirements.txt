fastapi
uvicorn[standard]
openai
yt-dlp
python-dotenv
requests
pydantic
beautifulsoup4
lxml
trafilatura
# Persistence layer — SQLAlchemy Core over SQLite (local/dev) or Postgres
# (Railway, via DATABASE_URL). psycopg (v3) is the Postgres driver.
SQLAlchemy>=2.0
psycopg[binary]>=3.1
# Auth: JWT signing/verification (RS256 for Apple/Google identity tokens needs
# the crypto extra), password hashing, and email validation.
PyJWT[crypto]>=2.8
argon2-cffi>=23.0
email-validator>=2.0
httpx>=0.27
